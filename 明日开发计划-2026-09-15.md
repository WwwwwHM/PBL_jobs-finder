# 简历诊断模块明日开发计划（2026-09-15）

## 1. 明日目标

将当前“主体代码已贯通”的简历诊断模块推进到“可按 M3 标准验收”的状态。明天不重复搭建已有流程，重点补齐真实输入、真实模型、异常处理和浏览器端证据。

当日完成定义：

1. 真实文本型 PDF 可以完成“上传 → 解析 → 诊断 → 保存 → 编辑优化稿 → 导出 Word”。
2. 扫描件、空白、加密、非 PDF、超过 10 MB 等输入均有明确且可继续操作的提示。
3. 模型超时、限流、空响应和非法 JSON 不产生脏记录，也不消耗用户配额。
4. 诊断报告稳定包含 0-100 评分、缺失关键词、可执行建议，以及基于原简历事实的 STAR 改写内容。
5. 全量自动化测试通过，并留下真实调用耗时和浏览器验收记录。

## 2. 当前基线

截至 9 月 14 日：

- 已实现 PDF/粘贴文本输入、目标岗位校验、智谱模型调用、结构化结果解析、SQLite 保存、每日配额、优化稿编辑和 Word 导出。
- 开发前基线为 28 个自动化测试；本轮已扩展至 41 个且全部通过。
- 尚无可用于验收的真实 PDF 样例。
- 本地尚未创建 `.env`，因此还没有完成真实智谱 API 联调。
- `requirements.txt` 已包含 `pdfplumber`，但解析器当前只使用 `PyPDF2`，设计中的双解析兜底尚未落地。
- 模型客户端当前没有显式超时、有限重试或 JSON 响应约束。
- 全仓 Ruff 当前仅有一个既存问题：`src/pbl_jobs_finder/__main__.py` 的导入块格式，不阻塞简历诊断验收。

## 3. 明日排期

| 时间 | 工作内容 | 交付与退出条件 |
| :--- | :--- | :--- |
| 09:00-09:30 | 开工检查 | 创建本地 `.env` 并配置有效 `ZHIPU_API_KEY`；准备匿名文本型 PDF、空白/扫描 PDF、加密 PDF、非 PDF 和大文件样例；重跑 28 个基线测试 |
| 09:30-11:00 | PDF 解析加固 | `PyPDF2` 提取为空或异常时调用 `pdfplumber`；统一换行和空白清洗；文件类型、大小、加密和空文本错误保持可读 |
| 11:00-12:00 | PDF 自动化测试 | 覆盖正常中文 PDF、多页 PDF、扩展名大小写、文件不存在、非 PDF、超过 10 MB、加密、空白/扫描件和 60000 字上限 |
| 13:30-14:45 | 模型调用与响应契约加固 | 增加显式超时、只针对瞬时故障的有限重试、结构化 JSON 约束或等价解析保护；确保建议或优化稿明确体现 STAR，且禁止伪造事实与数字 |
| 14:45-15:30 | 失败语义与配额测试 | 验证配置缺失、超时、限流、空响应、非法 JSON、数据库写入失败时不留记录且配额回滚；成功诊断只计 1 次，导出不计次 |
| 15:30-16:30 | 真实 API 与浏览器闭环 | 用匿名简历完成一次粘贴文本流程和一次 PDF 流程；检查加载、结果显隐、重新诊断、编辑、下载、重新登录和用户隔离 |
| 16:30-17:15 | 性能与输出质量检查 | 连续记录至少 3 次真实诊断耗时；目标单次不超过 30 秒；人工检查关键词、建议和 STAR 内容是否与岗位及原始事实一致 |
| 17:15-18:00 | 回归、修复与签收 | 全量测试、编译检查、定向 Ruff；更新 README/排期/已知限制；满足退出条件后将 M3 标记完成，否则记录阻塞项和责任任务 |

午休默认安排为 12:00-13:30，不计入开发工时。时间盒结束仍未完成时，按第 6 节优先级收敛范围。

## 4. 文件级任务清单

| 文件 | 明日改动 |
| :--- | :--- |
| `src/pbl_jobs_finder/modules/resume_parser.py` | 增加 `pdfplumber` 兜底与文本清洗；保持异常类型和中文提示统一 |
| `src/pbl_jobs_finder/utils/llm_client.py` | 增加超时、有限重试和可区分的服务异常；不在日志或界面泄露 API Key |
| `src/pbl_jobs_finder/modules/resume_diagnosis.py` | 收紧响应字段校验；明确 STAR 输出契约；验证失败时禁止写库 |
| `frontend.py` | 仅修复真实浏览器验收发现的问题；保留粘贴文本优先、失败后可继续编辑输入的行为 |
| `tests/test_resume_diagnosis.py` | 增加 PDF、模型异常、数据库/配额原子性和 STAR 契约测试 |
| `tests/test_frontend_auth.py` | 增加重新诊断、失败状态清理、登录失效和下载按钮状态测试 |
| `README.md`、`项目排期.md` | 写入真实验收结论、耗时数据和未解决限制 |

## 5. 验收矩阵

| 场景 | 预期结果 | 自动化 | 手工 |
| :--- | :--- | :---: | :---: |
| 正常中文文本型 PDF | 正确提取并完成诊断 | 是 | 是 |
| 正常粘贴文本 | 不依赖 PDF，完成同一闭环 | 是 | 是 |
| 同时上传和粘贴 | 明确采用粘贴文本 | 是 | 是 |
| 扫描件或空白 PDF | 提示改用文本粘贴，不扣配额 | 是 | 是 |
| 加密、非 PDF、超过 10 MB | 阻止调用模型并给出对应提示 | 是 | 是 |
| 未填写岗位、简历过短/过长 | 前置校验失败，不扣配额 | 是 | 否 |
| 模型超时、限流、空响应、非法 JSON | 无数据库记录，配额回滚，可重试 | 是 | 选测 |
| 正常模型响应 | 字段完整、分值合法、事实不新增 | 是 | 是 |
| 编辑后导出 Word | 导出的内容与最终编辑稿一致，不重复扣配额 | 是 | 是 |
| 另一账号导出当前记录 | 拒绝访问 | 是 | 是 |
| 重新诊断 | 新建独立记录并再计 1 次 | 是 | 是 |

## 6. 优先级与范围线

P0，明天必须完成：

- 正常 PDF 与粘贴文本的真实闭环。
- PDF 错误兜底与模型失败不扣配额。
- 输出字段、事实约束和 STAR 内容可验收。
- 自动化回归全绿，真实调用小于 30 秒或有明确性能阻塞记录。

P1，有余量再完成：

- 更细的 Word 排版优化。
- 更多模型输出格式兼容。
- 将内联 Prompt 迁移到独立配置文件。
- 增加超过最低要求的真实简历样本。

若 15:30 前仍未打通真实 API，停止非必要重构，优先定位配置、SDK兼容性和网络问题；若外部服务持续不可用，使用录制的脱敏响应完成本地闭环，但 M3 只标记“条件通过”，不得宣称真实 API 已验收。

## 7. 开工与收工命令

```powershell
Copy-Item .env.example .env
# 手动填写 .env 中的 ZHIPU_API_KEY，禁止提交该文件

.\.venv\Scripts\python.exe -m unittest discover -s tests -v
.\.venv\Scripts\python.exe -m compileall -q frontend.py src tests
.\.venv\Scripts\python.exe -m ruff check frontend.py src/pbl_jobs_finder/modules src/pbl_jobs_finder/utils tests
.\.venv\Scripts\python.exe frontend.py
```

浏览器验收地址默认是 `http://127.0.0.1:7860`。提交前再次确认 `.env`、真实简历、生成的数据库和导出文件均未进入版本控制。

## 8. 当日记录模板

收工时在本节填写：

- 自动化：`41 / 41（通过）`
- 真实 PDF：`通过 / 失败，样例类型`
- 真实 API：`通过 / 失败，模型名`
- 诊断耗时：`第1次 / 第2次 / 第3次 / 平均 / 最大`
- 配额回滚：`通过（模型失败、数据库写入失败）`
- 浏览器闭环：`页面与配置端点通过；真实诊断待配置 API Key 后验收`
- M3 结论：`条件通过`
- 遗留项：`尚未配置 ZHIPU_API_KEY，真实模型输出质量与 30 秒性能目标待验证`
