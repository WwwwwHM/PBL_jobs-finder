# 简历诊断模块今日开发计划（2026-09-15）

## 1. 今日目标

将当前“主体代码已贯通”的简历诊断模块推进到“可按 M3 标准验收”的状态，并补齐“生成新版简历前补充履历”的关键交互。今天不重复搭建已有流程，重点调整诊断与生成的职责边界，补齐真实输入、真实模型、异常处理和浏览器端证据。

当日完成定义：

1. 真实文本型 PDF 可以完成“上传 → 解析 → 诊断 → 打开补充履历窗口 → AI 生成新版稿 → 编辑 → 导出 Word”。
2. 扫描件、空白、加密、非 PDF、超过 10 MB 等输入均有明确且可继续操作的提示。
3. 模型超时、限流、空响应和非法 JSON 不产生脏记录，也不消耗用户配额。
4. 诊断报告稳定包含 0-100 评分、缺失关键词、可执行建议，以及基于原简历事实的 STAR 改写内容。
5. 用户在生成新版稿前可以取消、留空直接生成，或补充最多 6000 字的真实履历；失败后输入不丢失。
6. 智能体只整合原简历与用户补充的事实，不自行编造经历、技能、证书、时间或数字。
7. 全量自动化测试通过，并留下真实调用耗时和浏览器验收记录。

## 2. 当前基线

截至 9 月 14 日：

- 已实现 PDF/粘贴文本输入、目标岗位校验、智谱模型调用、结构化结果解析、SQLite 保存、每日配额、优化稿编辑和 Word 导出。
- 当前“开始诊断”会直接返回完整优化稿，“生成新版简历”仅负责导出 Word；尚无生成前补充履历的交互和第二阶段 AI 生成接口。
- 开发前基线为 28 个自动化测试；本轮已扩展至 41 个且全部通过。
- 尚无可用于验收的真实 PDF 样例。
- 本地尚未创建 `.env`，因此还没有完成真实智谱 API 联调。
- `requirements.txt` 已包含 `pdfplumber`，但解析器当前只使用 `PyPDF2`，设计中的双解析兜底尚未落地。
- 模型客户端当前没有显式超时、有限重试或 JSON 响应约束。
- 全仓 Ruff 当前仅有一个既存问题：`src/pbl_jobs_finder/__main__.py` 的导入块格式，不阻塞简历诊断验收。

## 3. 新增需求拆解

### 3.1 用户流程与交互状态

1. 用户完成诊断后点击“生成新版简历”，先打开“补充履历”窗口，不立即导出文件。
2. 窗口提供一个最多 6000 字的多行输入框，以及“取消”“暂无补充，直接生成”“确认补充并生成”三个动作。
3. 点击取消只关闭窗口，不改变当前优化稿；留空也允许生成，不强迫用户重复填写原简历已有内容。
4. 生成期间禁用重复提交并展示加载状态；成功后关闭窗口、刷新可编辑新版稿，并显示独立的“导出 Word”按钮。
5. 校验或模型失败时保留补充内容，用户修正或重试后再提交；退出登录、重新诊断或切换用户时清空窗口状态。

### 3.2 服务与数据契约

| 子任务 | 实现口径 | 退出条件 |
| :--- | :--- | :--- |
| 生成接口 | 新增按 `token + record_id + supplemental_experience` 生成新版稿的服务方法 | 先校验登录和记录归属，再读取原简历、岗位与当前优化稿；其他用户的记录不可访问 |
| 输入校验 | 补充履历可为空，去除首尾空白后最多 6000 字 | 超长输入在调用模型前失败，错误信息可读 |
| Prompt 组装 | 明确区分原简历、当前优化稿、目标岗位和补充履历 | 补充内容按不可信事实材料处理，其中的指令不得覆盖系统约束 |
| 事实边界 | 只能使用两份用户材料中已有的事实 | 缺少结果数字时保留“[请补充真实结果]”，不自动捏造量化成果 |
| 保存策略 | 补充履历只用于本次生成，不建立独立长期人才档案；成功后仅更新当前记录的 `optimized_text` | 模型或数据库失败时不覆盖上一版优化稿 |
| 配额语义 | 诊断成功计 1 次；同一诊断记录的新版稿生成、失败重试、编辑和导出不再次计次 | 自动化测试证明配额保持不变 |

### 3.3 测试拆解

- 后端：空补充、正常补充、6000 字边界、超长输入、记录不存在、跨用户访问、Prompt 指令注入、模型失败、数据库失败、成功更新优化稿和不重复扣配额。
- 前端：打开、取消、留空生成、确认生成、超长提示、加载中防重复、失败保留输入、成功刷新新版稿、导出按钮显隐，以及退出/重新诊断后的状态清理。
- 浏览器：分别跑通“无补充直接生成”和“补充项目成果后生成”两条路径，人工比对新版稿是否吸收补充事实且没有新增虚构信息。

### 3.4 本次范围边界

本次不做结构化多步骤履历表单、补充材料附件、个人履历知识库、多个补充版本管理或自动推测缺失数字。上述能力进入 MVP 后迭代池；今日只交付一个轻量窗口和一次生成闭环。

## 4. 今日排期

| 时间 | 工作内容 | 交付与退出条件 |
| :--- | :--- | :--- |
| 09:00-09:30 | 开工检查与契约确认 | 重跑自动化基线；准备匿名 PDF 与异常样例；确认弹窗字段、6000 字上限、保存策略、配额口径和按钮状态 |
| 09:30-10:30 | 原 M3 阻塞项加固 | 完成 PDF 双解析/文本清洗和模型超时、有限重试、结构化响应保护；正常与异常输入的提示保持可操作 |
| 10:30-11:30 | 新版稿生成服务 | 新增第二阶段生成方法；校验 Token、记录归属和补充内容；组装四段上下文；成功后原子更新 `optimized_text` |
| 11:30-12:00 | 后端定向测试 | 覆盖空值、边界、跨用户、模型/数据库失败、事实约束和配额不重复扣减；修复阻塞问题 |
| 13:30-14:45 | 补充履历窗口 | 将原“生成新版简历”改为打开窗口；接入取消、直接生成和确认生成；增加字符提示、加载、防重复提交和错误保留 |
| 14:45-15:30 | 新版稿审阅与导出链路 | 生成成功后回填可编辑新版稿；将 Word 导出改为独立确认动作；重新诊断、退出和切换用户时清理相关状态 |
| 15:30-16:30 | 前端与集成测试 | 覆盖窗口显隐、按钮状态、失败重试、结果刷新、导出内容和旧流程回归；全量自动化保持通过 |
| 16:30-17:15 | 真实 API 与浏览器验收 | 用匿名简历各跑一次无补充和有补充流程；验证事实吸收、无虚构、用户隔离、配额和错误恢复 |
| 17:15-18:00 | 性能、回归与签收 | 记录至少 3 次真实调用耗时；执行编译与定向 Ruff；更新 README、排期、验收结论和遗留项 |

午休默认安排为 12:00-13:30，不计入开发工时。时间盒结束仍未完成时，按第 7 节优先级收敛范围。

## 5. 文件级任务清单

| 文件 | 今日改动 |
| :--- | :--- |
| `src/pbl_jobs_finder/modules/resume_parser.py` | 增加 `pdfplumber` 兜底与文本清洗；保持异常类型和中文提示统一 |
| `src/pbl_jobs_finder/utils/llm_client.py` | 增加超时、有限重试和可区分的服务异常；不在日志或界面泄露 API Key |
| `src/pbl_jobs_finder/modules/resume_diagnosis.py` | 将诊断与新版稿生成拆为两个服务动作；增加补充履历校验、Prompt 事实边界、记录归属校验和成功后原子更新 |
| `frontend.py` | 增加补充履历窗口及其状态；拆分“AI 生成新版稿”和“导出 Word”；处理加载、防重复、失败保留与会话清理 |
| `tests/test_resume_diagnosis.py` | 增加 PDF、模型异常、补充履历边界、越权、Prompt 安全、原子更新、配额和 STAR 契约测试 |
| `tests/test_frontend_auth.py` | 增加窗口开关、三种提交路径、失败状态清理、登录失效、新版稿回填和下载按钮状态测试 |
| `README.md`、`项目排期.md` | 写入真实验收结论、耗时数据和未解决限制 |

## 6. 验收矩阵

| 场景 | 预期结果 | 自动化 | 手工 |
| :--- | :--- | :---: | :---: |
| 正常中文文本型 PDF | 正确提取并完成诊断 | 是 | 是 |
| 正常粘贴文本 | 不依赖 PDF，完成同一闭环 | 是 | 是 |
| 同时上传和粘贴 | 明确采用粘贴文本 | 是 | 是 |
| 扫描件或空白 PDF | 提示改用文本粘贴，不扣配额 | 是 | 是 |
| 加密、非 PDF、超过 10 MB | 阻止调用模型并给出对应提示 | 是 | 是 |
| 未填写岗位、简历过短/过长 | 前置校验失败，不扣配额 | 是 | 否 |
| 模型超时、限流、空响应、非法 JSON | 无数据库记录，配额回滚，可重试 | 是 | 选测 |
| 正常模型响应 | 字段完整、分值合法、事实不新增 | 是 | 是 |
| 点击生成新版简历后取消 | 窗口关闭，当前优化稿和记录均不改变 | 是 | 是 |
| 补充履历留空 | 可直接生成新版稿，不要求重复填写已有信息 | 是 | 是 |
| 补充有效履历 | 新版稿吸收相关事实，不执行补充文本中的指令，不虚构事实和数字 | 是 | 是 |
| 补充内容超过 6000 字 | 模型调用前提示精简，窗口内容保留 | 是 | 是 |
| 新版稿生成失败 | 保留上一版优化稿和补充输入，可重试且不重复扣配额 | 是 | 是 |
| 编辑后导出 Word | 导出的内容与最终编辑稿一致，不重复扣配额 | 是 | 是 |
| 另一账号导出当前记录 | 拒绝访问 | 是 | 是 |
| 重新诊断 | 新建独立记录并再计 1 次 | 是 | 是 |

## 7. 优先级与范围线

P0，今天必须完成：

- 正常 PDF 与粘贴文本的真实闭环。
- 补充履历窗口、第二阶段 AI 生成、可编辑新版稿和 Word 导出闭环。
- 补充输入校验、跨用户访问保护、生成失败保留输入且不覆盖上一版。
- PDF 错误兜底与模型失败不扣配额。
- 输出字段、事实约束和 STAR 内容可验收。
- 自动化回归全绿，真实调用小于 30 秒或有明确性能阻塞记录。

P1，有余量再完成：

- 更细的 Word 排版优化。
- 更多模型输出格式兼容。
- 将内联 Prompt 迁移到独立配置文件。
- 增加超过最低要求的真实简历样本。
- 结构化履历表单、补充附件、长期履历档案和多个补充版本。

若 15:30 前补充履历主链路仍未打通，停止 Word 排版和额外模型兼容工作，先保证“打开窗口 → 提交 → 生成新版稿 → 编辑 → 导出”的 P0 闭环。若 16:30 前仍未打通真实 API，优先定位配置、SDK 兼容性和网络问题；外部服务持续不可用时可用录制的脱敏响应完成本地闭环，但 M3 只标记“条件通过”，不得宣称真实 API 已验收。

## 8. 开工与收工命令

```powershell
Copy-Item .env.example .env
# 手动填写 .env 中的 ZHIPU_API_KEY，禁止提交该文件

.\.venv\Scripts\python.exe -m unittest discover -s tests -v
.\.venv\Scripts\python.exe -m compileall -q frontend.py src tests
.\.venv\Scripts\python.exe -m ruff check frontend.py src/pbl_jobs_finder/modules src/pbl_jobs_finder/utils tests
.\.venv\Scripts\python.exe frontend.py
```

浏览器验收地址默认是 `http://127.0.0.1:7860`。提交前再次确认 `.env`、真实简历、生成的数据库和导出文件均未进入版本控制。

## 9. 当日记录模板

收工时在本节填写：

- 自动化：`41 / 41（通过）`
- 真实 PDF：`通过 / 失败，样例类型`
- 真实 API：`通过 / 失败，模型名`
- 诊断耗时：`第1次 / 第2次 / 第3次 / 平均 / 最大`
- 配额回滚：`通过（模型失败、数据库写入失败）`
- 补充履历窗口：`取消 / 留空生成 / 补充后生成 / 失败重试 / 状态清理`
- 事实边界：`吸收的补充事实 / 未发现虚构项 / 待人工复核项`
- 浏览器闭环：`上传/粘贴 → 诊断 → 补充履历 → 生成新版稿 → 编辑 → Word 导出`
- M3 结论：`条件通过`
- 遗留项：`尚未配置 ZHIPU_API_KEY，真实模型输出质量与 30 秒性能目标待验证`
