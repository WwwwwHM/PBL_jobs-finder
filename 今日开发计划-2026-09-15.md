# 简历诊断模块今日开发计划（2026-09-15）

## 1. 今日目标

将当前“主体代码已贯通”的简历诊断模块推进到“可按 M3 标准验收”的状态，并补齐“生成新版简历前补充履历”的关键交互。今天不重复搭建已有流程，重点调整诊断与生成的职责边界，补齐真实输入、真实模型、异常处理和浏览器端证据。

当日完成定义：

1. 真实文本型 PDF 可以完成“上传 → 解析 → 诊断 → 打开补充履历窗口 → AI 输出 JSON → HTML 渲染 → Playwright 生成并下载 PDF”。
2. 扫描件、空白、加密、非 PDF、超过 12 MB 等输入均有明确且可继续操作的提示。
3. 模型超时、限流、空响应和非法 JSON 不产生脏记录，也不消耗用户配额。
4. 诊断报告稳定包含 0-100 评分、缺失关键词、可执行建议，以及基于原简历事实的 STAR 改写内容。
5. 用户在生成新版稿前可以取消、留空直接生成，或补充最多 6000 字的真实履历；失败后输入不丢失。
6. 智能体只整合原简历与用户补充的事实，不自行编造经历、技能、证书、时间或数字。
7. 新版 PDF 使用 A4 版式，中文、长经历分页、页边距和链接均可读，无裁切、重叠和黑块。
8. 全量自动化测试通过，并留下真实调用耗时、PDF 页面渲染和浏览器验收记录。

## 2. 当前基线

截至 9 月 14 日：

- 已实现 PDF/粘贴文本输入、目标岗位校验、智谱模型调用、结构化诊断结果解析、SQLite 保存、每日配额和当前优化稿编辑。
- 当前“开始诊断”返回可编辑诊断优化稿；“生成新版 PDF 简历”已拆为补充履历、结构化 JSON、固定 HTML/CSS 和 Playwright 打印链路。
- 开发前基线为 28 个自动化测试；本轮已扩展至 41 个且全部通过。
- 尚无可用于验收的真实 PDF 样例。
- 本地尚未创建 `.env`，因此还没有完成真实智谱 API 联调。
- `requirements.txt` 已包含 `pdfplumber` 和 `playwright==1.47.0`；Chromium 需要执行 `playwright install chromium`，生产环境需固定浏览器缓存目录。
- 模型客户端当前没有显式超时、有限重试或 JSON 响应约束。
- 全仓 Ruff 当前仅有一个既存问题：`src/pbl_jobs_finder/__main__.py` 的导入块格式，不阻塞简历诊断验收。

## 3. 新增需求拆解

### 3.1 用户流程与交互状态

1. 用户完成诊断后点击“生成新版简历”，先打开“补充履历”窗口，不立即导出文件。
2. 窗口提供一个最多 6000 字的多行输入框，以及“取消”“暂无补充，直接生成”“确认补充并生成”三个动作。
3. 点击取消只关闭窗口，不改变当前优化稿；留空也允许生成，不强迫用户重复填写原简历已有内容。
4. 生成期间禁用重复提交并展示加载状态；成功后关闭窗口并显示新版 PDF 下载按钮。
5. 校验或模型失败时保留补充内容，用户修正或重试后再提交；退出登录、重新诊断或切换用户时清空窗口状态。

### 3.2 服务与数据契约

| 子任务 | 实现口径 | 退出条件 |
| :--- | :--- | :--- |
| 生成接口 | 新增按 `token + record_id + supplemental_experience` 生成新版稿的服务方法 | 先校验登录和记录归属，再读取原简历、岗位与当前优化稿；其他用户的记录不可访问 |
| 输入校验 | 补充履历可为空，去除首尾空白后最多 6000 字 | 超长输入在调用模型前失败，错误信息可读 |
| Prompt 组装 | 明确区分原简历、当前优化稿、目标岗位和补充履历，要求只返回约定 JSON | 补充内容按不可信事实材料处理，其中的指令不得覆盖系统约束 |
| 事实边界 | 只能使用两份用户材料中已有的事实 | 缺少结果数字时保留“[请补充真实结果]”，不自动捏造量化成果 |
| 保存策略 | 补充履历只用于本次生成，不建立独立长期人才档案；成功后将规范化 JSON 保存到当前记录的 `optimized_text` | 模型、Schema、PDF 或数据库失败时不覆盖上一版优化稿 |
| 配额语义 | 诊断成功计 1 次；同一诊断记录的新版PDF生成、失败重试和下载不再次计次 | 自动化测试证明配额保持不变 |

### 3.3 PDF 技术路线拆解

| 层级 | 任务 | 约束与验收 |
| :--- | :--- | :--- |
| JSON Schema | 使用 Pydantic 定义 `basics`、`skills`、`experience`、`projects`、`education`、`certificates` | 拒绝非法 JSON、错误字段类型、超长字段和没有有效内容的文档；规范化后再持久化 |
| HTML 模板 | 使用服务端固定模板把结构化字段渲染为语义化 HTML | 所有模型和用户文本必须 HTML 转义；禁止模型直接提供 HTML、CSS、脚本或远程资源 |
| 打印样式 | 提供 A4 `@page`、中文字体栈、标题层级、间距、链接换行和 `break-inside` 规则 | 一页短简历不过度留白，多页长简历不裁切经历，不产生孤立分区标题 |
| Playwright | 使用 Python Playwright 启动无头 Chromium，`set_content` 后调用 `page.pdf` | `print_background=True`、`prefer_css_page_size=True`；浏览器或渲染失败转换为可操作错误 |
| 文件安全 | 输出到受控目录，文件名由岗位和记录 ID 生成 | 清理文件名，不接受用户路径；同一记录重复生成覆盖自己的稳定文件，不影响其他用户 |
| PDF 验证 | 使用 PDF 解析器检查可打开、页数和关键文本，再用 Poppler 渲染 PNG 做视觉验收 | 检查中文、字体、分页、边距、重叠、黑块和空白页；至少覆盖 1 页和多页样例 |

### 3.4 测试拆解

- 后端：空补充、正常补充、6000 字边界、超长输入、记录不存在、跨用户访问、Prompt 指令注入、模型失败、非法 JSON、Schema 失败、HTML 转义、Playwright 失败、数据库失败、成功保存 JSON 和不重复扣配额。
- 前端：打开、取消、留空生成、确认生成、超长提示、加载中防重复、失败保留输入、成功显示 PDF 下载，以及退出/重新诊断后的状态清理。
- PDF：一页、多页、中文、超长单词/链接、空可选分区和占位符样例；检查文件头、页数、关键文本并渲染所有页面做视觉检查。
- 浏览器：分别跑通“无补充直接生成”和“补充项目成果后生成”两条路径，人工比对 PDF 是否吸收补充事实且没有新增虚构信息。

### 3.5 本次范围边界

本次不做结构化多步骤履历表单、补充材料附件、个人履历知识库、多个补充版本管理、用户自定义模板或自动推测缺失数字。上述能力进入 MVP 后迭代池；今日只交付一个轻量窗口、一套固定 PDF 模板和一次生成闭环。

## 4. 今日排期

| 时间 | 工作内容 | 交付与退出条件 |
| :--- | :--- | :--- |
| 09:00-09:30 | 开工检查与依赖确认 | 重跑自动化基线；确认补充窗口契约；检查 Python Playwright、Chromium 和 Poppler；锁定依赖版本与安装步骤 |
| 09:30-10:30 | JSON Schema 与生成 Prompt | 定义 Pydantic 简历模型、长度边界和内容完整性校验；第二阶段模型只输出 JSON；覆盖非法响应和事实约束 |
| 10:30-11:30 | HTML/CSS 简历模板 | 实现字段转义、可选分区、A4 页面、中文字体、链接换行和分页规则；输出不包含远程资源或用户脚本 |
| 11:30-12:00 | Playwright PDF 生成器 | 无头 Chromium 打印 PDF；受控路径和稳定文件名；浏览器缺失、启动失败和写入失败均有明确异常 |
| 13:30-14:30 | 服务层闭环 | 校验 Token、记录归属和补充内容；串联 JSON → HTML → PDF；成功后保存规范化 JSON，失败不覆盖旧稿且不重复扣配额 |
| 14:30-15:30 | 补充履历窗口 | 将原导出按钮改为打开补充窗口；接入取消、直接生成和确认生成；处理加载、防重复、失败保留和 PDF 下载 |
| 15:30-16:30 | 自动化与集成测试 | 覆盖输入边界、越权、Prompt 注入、Schema、HTML 转义、Playwright、原子更新、配额和前端状态 |
| 16:30-17:15 | PDF 渲染与浏览器验收 | 生成一页和多页匿名简历；用解析器检查内容，用 Poppler 渲染全部页面并检查中文、分页、边距和重叠 |
| 17:15-18:00 | 性能、回归与签收 | 跑全量测试、编译与 Ruff；记录模型和 PDF 渲染耗时；更新 README、排期、验收结论和遗留项 |

午休默认安排为 12:00-13:30，不计入开发工时。时间盒结束仍未完成时，按第 7 节优先级收敛范围。

## 5. 文件级任务清单

| 文件 | 今日改动 |
| :--- | :--- |
| `src/pbl_jobs_finder/modules/resume_parser.py` | 增加 `pdfplumber` 兜底与文本清洗；保持异常类型和中文提示统一 |
| `src/pbl_jobs_finder/utils/llm_client.py` | 增加超时、有限重试和可区分的服务异常；不在日志或界面泄露 API Key |
| `src/pbl_jobs_finder/modules/resume_diagnosis.py` | 将诊断与新版稿生成拆为两个服务动作；增加补充履历校验、Prompt 事实边界、记录归属校验和成功后原子更新 |
| `src/pbl_jobs_finder/modules/resume_pdf.py` | 新增 Pydantic JSON Schema、固定 HTML/CSS 模板、安全转义、Markdown 预览和 Playwright A4 PDF 生成器 |
| `frontend.py` | 增加补充履历窗口及其状态；将原文件导出动作替换为 PDF 生成和下载；处理加载、防重复、失败保留与会话清理 |
| `tests/test_resume_diagnosis.py` | 增加补充履历边界、越权、Prompt 安全、Schema、HTML 转义、PDF 失败、原子更新和配额测试 |
| `tests/test_resume_pdf.py` | 增加 JSON 模型、HTML 模板、一页/多页 PDF、中文文本和分页回归测试 |
| `tests/test_frontend_auth.py` | 增加窗口开关、三种提交路径、失败状态清理、登录失效和 PDF 下载按钮状态测试 |
| `pyproject.toml`、`requirements.txt` | 加入 Python Playwright 依赖；记录 Chromium 安装命令，不引入远程字体或模板依赖 |
| `README.md`、`项目排期.md` | 写入真实验收结论、耗时数据和未解决限制 |

## 6. 验收矩阵

| 场景 | 预期结果 | 自动化 | 手工 |
| :--- | :--- | :---: | :---: |
| 正常中文文本型 PDF | 正确提取并完成诊断 | 是 | 是 |
| 正常粘贴文本 | 不依赖 PDF，完成同一闭环 | 是 | 是 |
| 同时上传和粘贴 | 明确采用粘贴文本 | 是 | 是 |
| 扫描件或空白 PDF | 提示改用文本粘贴，不扣配额 | 是 | 是 |
| 加密、非 PDF、超过 12 MB | 阻止调用模型并给出对应提示 | 是 | 是 |
| 未填写岗位、简历过短/过长 | 前置校验失败，不扣配额 | 是 | 否 |
| 模型超时、限流、空响应、非法 JSON | 无数据库记录，配额回滚，可重试 | 是 | 选测 |
| 正常模型响应 | 字段完整、分值合法、事实不新增 | 是 | 是 |
| 点击生成新版简历后取消 | 窗口关闭，当前优化稿和记录均不改变 | 是 | 是 |
| 补充履历留空 | 可直接生成新版稿，不要求重复填写已有信息 | 是 | 是 |
| 补充有效履历 | 新版稿吸收相关事实，不执行补充文本中的指令，不虚构事实和数字 | 是 | 是 |
| 补充内容超过 6000 字 | 模型调用前提示精简，窗口内容保留 | 是 | 是 |
| 新版稿生成失败 | 保留上一版优化稿和补充输入，可重试且不重复扣配额 | 是 | 是 |
| 非法 JSON 或 Schema 不合法 | 不渲染 HTML/PDF，提示重试，不覆盖旧稿 | 是 | 选测 |
| HTML 注入文本 | PDF 只显示文本，不执行标签、脚本或远程资源 | 是 | 是 |
| 一页/多页新版简历 | 生成可打开的 A4 PDF，中文、分页、边距和链接正常 | 是 | 是 |
| 另一账号生成当前记录的 PDF | 拒绝访问 | 是 | 是 |
| 重新诊断 | 新建独立记录并再计 1 次 | 是 | 是 |

## 7. 优先级与范围线

P0，今天必须完成：

- 正常 PDF 与粘贴文本的真实闭环。
- 补充履历窗口、结构化 JSON、固定 HTML 模板和 Playwright PDF 下载闭环。
- 补充输入校验、跨用户访问保护、生成失败保留输入且不覆盖上一版。
- PDF 错误兜底与模型失败不扣配额。
- 输出字段、事实约束和 STAR 内容可验收。
- 自动化回归全绿，真实调用小于 30 秒或有明确性能阻塞记录。

P1，有余量再完成：

- 第二套 PDF 模板和用户自定义配色。
- 更多模型输出格式兼容。
- 将内联 Prompt 迁移到独立配置文件。
- 增加超过最低要求的真实简历样本。
- 结构化履历表单、补充附件、长期履历档案和多个补充版本。

若 15:30 前补充履历主链路仍未打通，停止第二套版式和额外模型兼容工作，先保证“打开窗口 → JSON → HTML → PDF → 下载”的 P0 闭环。若 Playwright 浏览器安装受阻，不改用模型直接输出 HTML，也不伪造已验收结论；先完成可注入浏览器工厂的生成器和自动化测试，将真实 Chromium 验收记为阻塞。若 16:30 前仍未打通真实 API，可用录制的脱敏 JSON 完成本地 PDF 闭环，但 M3 只标记“条件通过”。

## 8. 开工与收工命令

```powershell
Copy-Item .env.example .env
# 手动填写 .env 中的 ZHIPU_API_KEY，禁止提交该文件

.\.venv\Scripts\python.exe -m unittest discover -s tests -v
.\.venv\Scripts\python.exe -m compileall -q frontend.py src tests
.\.venv\Scripts\python.exe -m ruff check frontend.py src/pbl_jobs_finder/modules src/pbl_jobs_finder/utils tests
.\.venv\Scripts\python.exe frontend.py
```

浏览器验收地址默认是 `http://127.0.0.1:7860`。提交前再次确认 `.env`、真实简历、生成的数据库和导出文件均未进入版本控制。

## 9. 当日执行记录

- 自动化：`62 / 62（通过，包含真实 Chromium 多页 PDF 集成测试）`
- 静态检查：`compileall 通过；Ruff 通过；git diff --check 通过`
- PDF 输入：`文本型、扫描件 OCR、空白、加密、非 PDF、超限场景自动化通过；匿名真实业务样本待验收`
- 真实 API：`未执行；真实智谱模型输出质量与响应耗时待有效 ZHIPU_API_KEY 验收`
- 配额：`诊断成功计 1 次；新版 PDF 生成、模型失败和 PDF 失败均不重复扣减`
- 补充履历窗口：`打开、取消、留空生成、补充后生成、失败保留、退出清理均有自动化覆盖`
- 事实边界：`Prompt 不可信数据边界、JSON Schema 拒绝额外字段、HTML 转义均通过；真实模型事实一致性待人工复核`
- PDF 结构检查：`%PDF 文件头、A4、3 页、关键文本可提取、无 JavaScript、未加密`
- PDF 视觉检查：`单页和 3 页中文样例均已用 Poppler 渲染；无裁切、重叠、黑块或孤立标题，页码正常`
- 应用启动：`Gradio 事件图构建通过；http://127.0.0.1:7860 返回 200`
- M3 结论：`条件通过`
- 遗留项：`补充真实 ZHIPU_API_KEY 后完成两条真实生成路径、30 秒性能目标和事实一致性人工验收`
